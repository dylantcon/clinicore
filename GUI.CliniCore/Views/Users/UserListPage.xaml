<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:GUI.CliniCore.ViewModels.Users"
             xmlns:converters="clr-namespace:GUI.CliniCore.Converters"
             xmlns:icons="clr-namespace:GUI.CliniCore.Resources.Fonts"
             xmlns:shared="clr-namespace:GUI.CliniCore.Views.Shared"
             x:Class="GUI.CliniCore.Views.Users.UserListPage"
             x:DataType="viewModels:UserListViewModel"
             Title="{Binding Title}">

    <!-- Configure Shell back button to trigger Back command -->
    <Shell.BackButtonBehavior>
        <BackButtonBehavior Command="{Binding BackCommand}"/>
    </Shell.BackButtonBehavior>

    <!-- Page Resources -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IsStringNotNullOrEmptyConverter x:Key="IsStringNotNullOrEmptyConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <!-- Outer grid to constrain max width on wide screens -->
    <Grid HorizontalOptions="Center" MaximumWidthRequest="1200">
        <Grid RowDefinitions="Auto,*" Padding="10">

        <!-- Search and Actions Bar -->
        <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,0,0,10">

            <!-- Back Button -->
            <Border BackgroundColor="#607D8B" StrokeThickness="0" Padding="12,10" StrokeShape="RoundRectangle 8" HeightRequest="40">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BackCommand}"/>
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="{x:Static icons:MaterialIcons.ArrowBack}" FontFamily="MaterialIconsRegular" FontSize="18" TextColor="White" VerticalOptions="Center"/>
                    <Label Text="Back to Dashboard" TextColor="White" FontSize="14" VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>

            <!-- Search Box -->
            <Border Stroke="LightGray" StrokeThickness="1" Background="White" StrokeShape="RoundRectangle 8" Padding="10">
                <Grid ColumnDefinitions="*,100" ColumnSpacing="10">
                    <Entry Grid.Column="0"
                           Placeholder="Search users by name or username"
                           Text="{Binding SearchText}"
                           TextColor="#333"
                           ClearButtonVisibility="WhileEditing"/>
                    <Button Grid.Column="1"
                            Text="Search"
                            BackgroundColor="#2196F3"
                            TextColor="White"/>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <Button Text="Create New Administrator"
                    Command="{Binding CreateAdminCommand}"
                    BackgroundColor="#F44336"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="45"/>

        </VerticalStackLayout>

        <!-- User List with SortableListView -->
        <shared:SortableListView Grid.Row="1"
                                  ItemsSource="{Binding Users}"
                                  SortOptions="{Binding SortOptions}"
                                  SelectedSortOption="{Binding SelectedSortOption}"
                                  IsAscending="{Binding IsAscending}"
                                  SelectedItem="{Binding SelectedUser}"
                                  RefreshCommand="{Binding RefreshCommand}"
                                  IsRefreshing="{Binding IsRefreshing}"
                                  EmptyMessage="No users found">
            <shared:SortableListView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:UserDisplayModel">
                        <Border Stroke="LightGray"
                                StrokeThickness="1"
                                Background="White"
                                StrokeShape="RoundRectangle 8"
                                Padding="15"
                                Margin="0,5">

                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">

                                <!-- User Info -->
                                <VerticalStackLayout Grid.Column="0" Spacing="5">

                                    <!-- User Name -->
                                    <Label Text="{Binding Name}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           TextColor="#333"/>

                                    <!-- Username -->
                                    <HorizontalStackLayout Spacing="5">
                                        <Label Text="Username:"
                                               FontSize="12"
                                               TextColor="Gray"
                                               FontAttributes="Bold"/>
                                        <Label Text="{Binding Username}"
                                               FontSize="12"
                                               TextColor="Gray"/>
                                    </HorizontalStackLayout>

                                    <!-- Additional Info -->
                                    <Label Text="{Binding AdditionalInfo}"
                                           FontSize="12"
                                           TextColor="Gray"
                                           IsVisible="{Binding AdditionalInfo, Converter={StaticResource IsStringNotNullOrEmptyConverter}}"/>

                                    <!-- User ID (Truncated) -->
                                    <Label FontSize="10" TextColor="LightGray">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="ID: " TextColor="LightGray"/>
                                                <Span Text="{Binding Id, StringFormat='{0:N}'}" TextColor="LightGray"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                </VerticalStackLayout>

                                <!-- Role Badge -->
                                <Border Grid.Column="1"
                                        BackgroundColor="{Binding RoleBadgeColor}"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 12"
                                        Padding="12,6"
                                        VerticalOptions="Start">
                                    <Label Text="{Binding RoleDisplayName}"
                                           TextColor="White"
                                           FontSize="12"
                                           FontAttributes="Bold"/>
                                </Border>

                            </Grid>

                        </Border>
                    </DataTemplate>
            </shared:SortableListView.ItemTemplate>
        </shared:SortableListView>

        <!-- Validation Errors -->
        <CollectionView Grid.Row="1"
                       ItemsSource="{Binding ValidationErrors}"
                       IsVisible="{Binding HasValidationErrors}"
                       VerticalOptions="Start"
                       Margin="10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="#F44336"
                            StrokeThickness="1"
                            Background="#FFEBEE"
                            StrokeShape="RoundRectangle 8"
                            Padding="10"
                            Margin="0,2">
                        <Label Text="{Binding .}"
                               TextColor="#D32F2F"
                               FontSize="12"/>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        </Grid>
    </Grid>

</ContentPage>
