<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:GUI.CliniCore.ViewModels.Patients"
             xmlns:domain="clr-namespace:Core.CliniCore.Domain.Users.Concrete;assembly=Core.CliniCore"
             xmlns:icons="clr-namespace:GUI.CliniCore.Resources.Fonts"
             xmlns:converters="clr-namespace:GUI.CliniCore.Converters"
             xmlns:shared="clr-namespace:GUI.CliniCore.Views.Shared"
             x:Class="GUI.CliniCore.Views.Patients.PatientListPage"
             x:DataType="viewModels:PatientListViewModel"
             Title="{Binding Title}">

    <!-- Configure Shell back button to trigger Back command -->
    <Shell.BackButtonBehavior>
        <BackButtonBehavior Command="{Binding BackCommand}"/>
    </Shell.BackButtonBehavior>

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:PhysicianAssignmentConverter x:Key="PhysicianAssignmentConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <!-- Outer grid to constrain max width on wide screens while filling on narrow screens -->
    <Grid HorizontalOptions="Fill" MaximumWidthRequest="1200">
        <Grid RowDefinitions="Auto,*" Padding="10" HorizontalOptions="Fill">

        <!-- Search and Actions Bar -->
        <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,0,0,10">

            <!-- Back Button -->
            <Border BackgroundColor="#607D8B" StrokeThickness="0" Padding="12,10" StrokeShape="RoundRectangle 8" HeightRequest="40">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BackCommand}"/>
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="{x:Static icons:MaterialIcons.ArrowBack}" FontFamily="MaterialIconsRegular" FontSize="18" TextColor="White" VerticalOptions="Center"/>
                    <Label Text="Back to Dashboard" TextColor="White" FontSize="14" VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>

            <!-- Search Box -->
            <Border Stroke="LightGray" StrokeThickness="1" Background="White" StrokeShape="RoundRectangle 8" Padding="10">
                <Grid ColumnDefinitions="*,100" ColumnSpacing="10">
                    <Entry Grid.Column="0"
                           Placeholder="Search patients by name or username"
                           Text="{Binding SearchText}"
                           TextColor="#333"
                           ClearButtonVisibility="WhileEditing"/>
                    <Button Grid.Column="1"
                            Text="Search"
                            Command="{Binding SearchCommand}"
                            BackgroundColor="#2196F3"
                            TextColor="White"/>
                </Grid>
            </Border>

            <!-- Physician Selector (Admin Only) - Used for assignment, not filtering -->
            <Border Stroke="#2196F3"
                    StrokeThickness="2"
                    Background="White"
                    StrokeShape="RoundRectangle 8"
                    Padding="10"
                    HorizontalOptions="Fill"
                    IsVisible="{Binding ShowPhysicianFilter}">
                <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto" RowSpacing="5">
                    <shared:StyledPicker Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                                         LabelText="Select Physician for Assignment"
                                         ItemsSource="{Binding AvailablePhysicians}"
                                         SelectedItem="{Binding SelectedPhysicianFilter}"
                                         ItemDisplayPath="Name"/>
                    <Button Grid.Row="1" Grid.Column="1"
                            Text="Clear Selection"
                            Command="{Binding ClearPhysicianFilterCommand}"
                            BackgroundColor="#333333"
                            TextColor="White"
                            FontSize="10"
                            Padding="8,4"
                            CornerRadius="4"
                            HeightRequest="28"
                            HorizontalOptions="End"/>
                </Grid>
            </Border>

            <!-- My Patients Toggle (Physician Only) -->
            <Border Stroke="#4CAF50"
                    StrokeThickness="2"
                    Background="White"
                    StrokeShape="RoundRectangle 8"
                    Padding="10"
                    HorizontalOptions="Fill"
                    MinimumWidthRequest="300"
                    IsVisible="{Binding ShowMyPatientsToggle}">
                <HorizontalStackLayout Spacing="10" MinimumWidthRequest="280">
                    <CheckBox IsChecked="{Binding ShowMyPatientsOnly}"/>
                    <Label Text="Show my patients only"
                           VerticalOptions="Center"
                           TextColor="#333"/>
                </HorizontalStackLayout>
            </Border>

            <!-- Assignment Status Filter -->
            <Border Stroke="#FF9800"
                    StrokeThickness="2"
                    Background="White"
                    StrokeShape="RoundRectangle 8"
                    Padding="10"
                    HorizontalOptions="Fill">
                <shared:StyledPicker LabelText="Assignment Status"
                                     SelectedIndex="{Binding AssignmentFilterIndex}">
                    <shared:StyledPicker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>All Patients</x:String>
                            <x:String>Assigned Only</x:String>
                            <x:String>Unassigned Only</x:String>
                        </x:Array>
                    </shared:StyledPicker.ItemsSource>
                </shared:StyledPicker>
            </Border>

            <!-- Action Buttons (RBAC: Only visible to authorized users) -->
            <Button Text="Create New Patient"
                    Command="{Binding CreatePatientCommand}"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="45"
                    IsVisible="{Binding CanCreatePatient}"/>

        </VerticalStackLayout>

        <!-- Patient List with SortableListView -->
        <shared:SortableListView Grid.Row="1"
                                  ItemsSource="{Binding Patients}"
                                  SortOptions="{Binding SortOptions}"
                                  SelectedSortOption="{Binding SelectedSortOption}"
                                  IsAscending="{Binding IsAscending}"
                                  SelectedItem="{Binding SelectedPatient}"
                                  RefreshCommand="{Binding RefreshCommand}"
                                  IsRefreshing="{Binding IsRefreshing}"
                                  EmptyMessage="No patients found">
            <shared:SortableListView.ItemTemplate>
                    <DataTemplate x:DataType="domain:PatientProfile">
                        <Border Stroke="LightGray"
                                StrokeThickness="1"
                                Background="White"
                                StrokeShape="RoundRectangle 8"
                                Padding="15"
                                Margin="0,5"
                                HorizontalOptions="Fill">

                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">

                                <!-- Patient Info -->
                                <VerticalStackLayout Grid.Column="0" Spacing="5">

                                    <!-- Patient Name -->
                                    <Label Text="{Binding Name}"
                                           FontSize="18"
                                           FontAttributes="Bold"
                                           TextColor="#333"/>

                                    <!-- Username -->
                                    <HorizontalStackLayout Spacing="5">
                                        <Label Text="Username:"
                                               FontSize="12"
                                               TextColor="Gray"
                                               FontAttributes="Bold"/>
                                        <Label Text="{Binding Username}"
                                               FontSize="12"
                                               TextColor="Gray"/>
                                    </HorizontalStackLayout>

                                    <!-- Age and Gender -->
                                    <HorizontalStackLayout Spacing="10">
                                        <Label FontSize="12" TextColor="Gray">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="Age: " FontAttributes="Bold"/>
                                                    <Span Text="{Binding BirthDate, StringFormat='{0:yyyy-MM-dd}'}"/>
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                        <Label FontSize="12" TextColor="Gray">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="Gender: " FontAttributes="Bold"/>
                                                    <Span Text="{Binding Gender}"/>
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                    </HorizontalStackLayout>

                                    <!-- Primary Physician Assignment Status -->
                                        <Border BackgroundColor="#2E7D32"
                                            StrokeThickness="0"
                                            StrokeShape="RoundRectangle 6"
                                            Padding="8,4"
                                            HorizontalOptions="Start"
                                            Margin="0,5,0,0">
                                            <Label FontSize="11" TextColor="#E8F5E9">
                                                <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="{x:Static icons:MaterialIcons.LocalHospital}"
                                                          FontFamily="MaterialIconsRegular"
                                                          FontSize="12"/>
                                                    <Span Text=" PCP: " FontAttributes="Bold"/>
                                                    <Span Text="{Binding PrimaryPhysicianId,
                                                                         Converter={StaticResource PhysicianAssignmentConverter}}"
                                                          FontAttributes="Bold"/>
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                    </Border>

                                    <!-- Patient ID (Truncated) -->
                                    <Label FontSize="10" TextColor="LightGray">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="ID: "/>
                                                <Span Text="{Binding Id, StringFormat='{0:N}'}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                </VerticalStackLayout>

                                <!-- Assign Button -->
                                <Button Grid.Column="1"
                                        Text="Assign"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:PatientListViewModel}}, Path=AssignPatientCommand}"
                                        CommandParameter="{Binding Id}"
                                        IsVisible="{Binding Source={RelativeSource AncestorType={x:Type viewModels:PatientListViewModel}}, Path=CanAssignPatients}"
                                        BackgroundColor="#FF9800"
                                        TextColor="White"
                                        CornerRadius="6"
                                        WidthRequest="80"
                                        HeightRequest="40"
                                        VerticalOptions="Center"/>

                            </Grid>

                        </Border>
                    </DataTemplate>
            </shared:SortableListView.ItemTemplate>
        </shared:SortableListView>

        <!-- Validation Errors -->
        <CollectionView Grid.Row="1"
                       ItemsSource="{Binding ValidationErrors}"
                       IsVisible="{Binding HasValidationErrors}"
                       VerticalOptions="Start"
                       Margin="10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="#F44336"
                            StrokeThickness="1"
                            Background="#FFEBEE"
                            StrokeShape="RoundRectangle 8"
                            Padding="10"
                            Margin="0,2">
                        <Label Text="{Binding .}"
                               TextColor="#D32F2F"
                               FontSize="12"/>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        </Grid>
    </Grid>

</ContentPage>
